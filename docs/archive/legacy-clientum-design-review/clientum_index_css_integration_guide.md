# Exact Line Changes for `index.css` Integration

To fully integrate the Clientum CRM Design System (`index.css`) with CollabWorkspace Pro, you need to update specific configuration blocks, color tokens, and theme selectors. 

Here are the exact sections and line modifications required:

---

## 1. Update the Tailwind Theme Directives (Lines 10–25)
Replace or update the `@theme` section so that Tailwind's utility classes (`bg-blue-600`, `text-blue-700`, etc.) map directly to Clientum's official corporate navy palette:

```css
@theme {
  --color-blue-50: #eef1f6;
  --color-blue-100: #dbe2ec;
  --color-blue-200: #c3cee0;
  --color-blue-300: #a9bfe0;
  --color-blue-400: #5b7ebd;
  --color-blue-500: #3d5f99;
  --color-blue-600: #0a2558; /* Official Clientum Brand Navy */
  --color-blue-700: #071b3d;
  --color-blue-800: #051530;
  --color-blue-900: #030f24;
  --color-blue-950: #010a18;
}
```

---

## 2. Map Light Mode Surface and Canvas Tokens (Lines 30–65)
Ensure your `:root`, `[data-theme="light"]`, and `.light` declarations define the high-contrast light surfaces:

```css
:root,
[data-theme="light"],
.light {
  --bg-canvas: #f8fafc;          /* Brighter, cleaner corporate base */
  --bg-surface: #ffffff;
  --bg-card: #ffffff;
  --text-primary: #0f172a;       /* Slate 900 for high readability */
  --text-secondary: #475569;     /* Slate 600 */
  --border-subtle: #e2e8f0;      /* Slate 200 */
  --color-primary: #0a2558;      /* Brand Navy */
}
```

---

## 3. Configure Obsidian Dark Mode Selectors (Lines 140–190)
Ensure your dark mode selectors (`[data-theme="dark"]` and `.dark`) map to the deep obsidian canvas to give CollabWorkspace Pro its professional dark aesthetic:

```css
[data-theme="dark"],
.dark {
  --bg-canvas: #040711;          /* Clientum Obsidian Dark */
  --bg-surface: #0a0f1d;
  --bg-card: #0d1527;
  --text-primary: #ffffff;       /* WCAG AAA High Contrast */
  --text-secondary: #cbd5e1;
  --border-subtle: #141f33;
  --color-primary: #1e40af;
}
```

---

## 4. Add High-Contrast Sidebar Styling Rules (Lines 230–260)
Verify that the `#clientum-sidebar` rules are included so that CollabWorkspace Pro's sidebar inherits the exact background colors and borders:

```css
#clientum-sidebar {
  background-color: var(--sidebar-bg) !important;
  border-right-color: var(--sidebar-border) !important;
  color: var(--sidebar-text-primary);
}
```

*Generated by Gemini 3 Flash.*